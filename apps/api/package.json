{
    "name": "@arc/api",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun --env-file=.env --watch src/index.ts",
        "build": "bun build src/index.ts --outdir dist --target bun",
        "check": "tsc --noEmit"
    },
    "dependencies": {
        "@arc/api-contract": "workspace:*",
        "@arc/backend": "workspace:*",
        "@arc/db": "workspace:*",
        "@arc/shared": "workspace:*",
        "@hono/zod-validator": "^0.9.0",
        "better-auth": "1.6.22",
        "drizzle-orm": "catalog:",
        "hono": "^4.13.3",
        "zod": "^4.4.3"
    },
    "devDependencies": {
        "@types/bun": "^1.4.0",
        "typescript": "~6.0.3"
    }
}
