DATABASE_URL=postgresql://arc:arc@localhost:5433/arc

TMDB_READ_ACCESS_TOKEN=

# Ordered, comma-separated fallback chain.
PLAYBACK_PROVIDERS=allanime,anikoto,anineko,senshi

# Optional providers that currently require a managed-challenge
# session can still be added to PLAYBACK_PROVIDERS.
ANIDB_COOKIE=
ANIMEPAHE_COOKIE=

# Bearer token for POST /api/internal/episodes/refresh
EPISODE_SYNC_TOKEN=
