{
    "name": "@arc/tests",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "scripts": {
        "check": "tsc --noEmit",
        "test": "bun test --isolate"
    },
    "dependencies": {
        "@arc/core": "workspace:*",
        "@arc/shared": "workspace:*",
        "dotenv": "catalog:",
        "drizzle-orm": "catalog:"
    },
    "devDependencies": {
        "@types/bun": "^1.4.0",
        "msw": "^2.15.0",
        "postgres": "catalog:"
    }
}
